# Must match the environment the model artifact was trained in.
# A sklearn version mismatch makes joblib.load() succeed but predict_proba() fail.
pandas==3.0.5
numpy==2.5.2
scipy==1.18.1
scikit-learn==1.9.0
joblib==1.6.0
fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.5
